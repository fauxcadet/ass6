a = input("Enter hyphen-separated sequence\n")

def sequence(x):
    a1 = x.replace("-", " ")

    a2 = a1.split()

    a3 = sorted(a2)

    string = ""

    for i in a3:
        string = string + i + "-"

    print(string)

sequence(a)
